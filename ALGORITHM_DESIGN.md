# 过滤条件发现算法设计文档

## 🎯 核心本质

**问题转化**：将过滤条件的排查问题转化为**集合操作问题**。

### 核心思路
1. **从主表共有行提取规律**：确保规律能100%覆盖所有主表共有行
2. **评估对主表独有行的影响**：最小化对主表独有行的误伤
3. **利用集合关系**：通过集合的包含关系和交集操作，避免组合爆炸和无效计算

## 🔍 全流程概览

```
┌───────────┐     ┌────────────────┐     ┌────────────────┐     ┌────────────────┐
│ Sheet数据 │ ──→ │ 过滤条件链生成 │ ──→ │ 支配关系森林构建 │ ──→ │ 推荐过滤条件组合 │
└───────────┘     └────────────────┘     └────────────────┘     └────────────────┘
```

### 阶段一：数据准备
- **输入**：两个Sheet的数据
- **输出**：共有行集合、主表独有行集合、参考表独有行集合
- **核心操作**：基于主键的匹配

### 阶段二：过滤条件链生成
- **输入**：单个Sheet的所有行、共有行集合
- **输出**：每个非主键字段的过滤条件链
- **核心操作**：模式提取（等于、枚举、前缀、前缀枚举）

### 阶段三：支配关系森林构建
- **输入**：所有字段的过滤条件链
- **输出**：支配关系森林
- **核心操作**：集合包含关系判断、树结构构建

### 阶段四：推荐过滤条件组合
- **输入**：支配关系森林
- **输出**：最优过滤条件组合
- **核心操作**：集合交集算法

## 📋 阶段一：数据准备

### 基于主键的匹配
1. **读取两个Sheet的数据**
2. **基于主键字段**，识别：
   - **主表共有行**：主表中与参考表存在相同主键的行
   - **主表独有行**：只在主表存在的行（参考表中无对应主键）
   - **参考表共有行**：参考表中与主表存在相同主键的行
   - **参考表独有行**：只在参考表存在的行（主表中无对应主键）

### 重要说明
- **共有行对应关系**：主表和参考表的共有行通过主键一一对应，行数必定一致
- **字段差异处理**：主表和参考表的字段名、字段数量可能不同，但共有行的主键值相同
- **独立分析**：对主表和参考表分别进行过滤条件分析，生成各自的过滤条件链

### 数据结构
- **RowSet**：基于位图实现的集合，支持高效的集合运算
  - `SubsetOf()`：子集判断
  - `Union()`：并集运算
  - `Subtract()`：差集运算
  - `Intersect()`：交集运算

## 🔗 阶段二：过滤条件链生成

### 核心目标
对每个非主键字段，生成多个可能的过滤规律，按误伤程度排序。

### 模式提取（四种最常见模式）

#### 1. 等于模式（Field = Value）
- **逻辑**：检查单个值是否覆盖所有主表共有行
- **示例**：`部门 = "技术部"`
- **D集合**：误伤的主表独有行集合

#### 2. 枚举模式（Field ∈ {Value1, Value2, ...}）
- **逻辑**：检查值集合是否覆盖所有主表共有行
- **示例**：`部门 ∈ {"技术部", "研发部"}`
- **D集合**：误伤的主表独有行集合

#### 3. 前缀模式（Field = Prefix*）
- **逻辑**：检查前缀是否覆盖所有主表共有行
- **示例**：`编码 = "A*"`
- **D集合**：误伤的主表独有行集合

#### 4. 前缀枚举模式（Field ∈ {Prefix1*, Prefix2*, ...}）
- **逻辑**：检查前缀集合是否覆盖所有主表共有行
- **示例**：`编码 ∈ {"A*", "B*"}`
- **D集合**：误伤的主表独有行集合

### 过滤条件链构建
1. **生成所有可能的模式**
2. **过滤**：只保留覆盖所有主表共有行的模式
3. **排序**：按D集合大小（误伤程度）升序排列
4. **去重**：D集合相同的模式只保留最简单的
5. **限制**：每个字段最多保留4个最优模式

## 🌲 阶段三：支配关系森林构建

### 核心思想
- **支配关系**：节点A支配节点B，当且仅当A的D集合是B的D集合的子集
- **根节点**：不被任何其他节点支配的节点
- **树**：一个根节点及其所有被支配节点
- **森林**：所有树的集合

### 构建过程
1. **识别根节点**：找到所有非被支配的节点
2. **构建树结构**：为每个根节点构建包含其所有被支配节点的树
3. **树的排序**：按根节点D集合大小升序排列

### 巧妙之处
- **避免组合爆炸**：通过支配关系，直接排除被支配的次优节点
- **减少无效计算**：只处理非被支配的根节点
- **层次化组织**：将相关节点组织成树结构，便于后续处理

## ✨ 阶段四：推荐过滤条件组合

### 核心目标
从支配关系森林中选择最优的过滤条件组合，最小化误伤。

### 算法步骤
1. **选择候选节点**：从每棵树中选择最优的节点（通常是根节点）
2. **计算组合**：生成候选节点的组合
3. **评估组合**：计算每个组合的总误伤（D集合的并集）
4. **选择最优**：选择总误伤最小的组合

### 集合交集算法
- **问题**：找到多个过滤条件的组合，其对主表独有行的误伤最小
- **解决方案**：利用支配关系森林，只考虑非被支配的节点，大幅减少需要评估的组合数

## 🚀 技术实现要点

### 1. RowSet位图实现
- **优势**：O(1)空间复杂度，高效的集合运算
- **应用**：所有集合操作的基础

### 2. 模式提取优化
- **预处理**：字段统计，避免重复遍历
- **剪枝**：只考虑与主表共有行有交集的值
- **限制**：枚举和前缀枚举最多10个元素

### 3. 支配关系判断
- **高效算法**：利用RowSet的SubsetOf()方法
- **复杂度**：O(n²)，但n通常很小

### 4. 组合生成策略
- **启发式**：优先选择D集合小的节点
- **剪枝**：排除明显次优的组合

## 📊 性能分析

### 时间复杂度
- **模式提取**：O(m × k)，m为行数，k为字段数
- **支配关系判断**：O(n²)，n为总节点数
- **组合生成**：O(t^p)，t为每棵树的候选节点数，p为树的数量

### 空间复杂度
- **RowSet**：O(m)，m为行数
- **节点存储**：O(k × 4)，每个字段最多4个节点

## 💡 核心优势

1. **问题转化**：将复杂的过滤条件排查转化为清晰的集合操作
2. **避免组合爆炸**：通过支配关系巧妙减少无效计算
3. **层次化处理**：从字段级到森林级再到组合级，逐步优化
4. **最小化误伤**：核心目标是最小化对独有行的误伤
5. **可扩展性**：容易添加新的模式提取方法

## 🎯 适用场景

- **数据比对**：快速发现两个数据集的差异规律
- **数据清洗**：自动生成过滤条件，分离不同类别的数据
- **规则发现**：从数据中自动提取业务规则
- **SQL生成**：基于发现的规律自动生成SQL过滤条件

## 🔮 未来扩展方向

- **更多模式**：添加范围模式、正则模式等
- **多字段组合**：支持跨字段的复杂组合
- **智能权重**：基于字段重要性的加权评估
- **交互式调优**：允许用户调整参数和规则