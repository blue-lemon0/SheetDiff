# 过滤条件发现算法设计文档

## 🎯 核心目标

从两个Excel Sheet中，基于主键匹配结果，自动发现能够解释"共有行"和"独有行"差异的过滤条件。

## 📊 数据模型

### 输入数据
- **主表数据**: 包含所有行的数据
- **参考表数据**: 包含所有行的数据
- **主键配置**: 用于匹配的字段列表
- **字段映射**: 主表字段 ↔ 参考表字段的对应关系

### 匹配结果
```
主表匹配结果:
├── 共有行（Matched）: 主表和参考表都存在（主键相同）
└── 独有行（OnlyMain）: 只在主表存在

参考表匹配结果:
├── 共有行（Matched）: 主表和参考表都存在（主键相同）
└── 独有行（OnlyRef）: 只在参考表存在
```

## 🔍 算法流程

### 第一步：独立分析每个Sheet

对**主表**和**参考表**分别执行以下分析（完全独立）：

```
输入：
- rows: 该Sheet的所有数据行（包含行号）
- commonSet: 共有行的行号集合
- 所有非主键字段列表

输出：
- 每个字段的 FieldChain（字段链条）
```

### 第二步：字段级别的规律提炼

对每个**非主键字段**，分析4种规律：

#### 规律1: 等于规律（Field = Value）
```
检查：某个单值能否覆盖所有共有行

示例：
  部门 = "技术部"
  → 覆盖行：0,1,2（都是共有行）
  → D集合：空（完美）✅
```

#### 规律2: 枚举规律（Field ∈ {Val1, Val2, ...}）
```
检查：一个小的值集合（≤10个）能否覆盖所有共有行

示例：
  部门 ∈ {"技术部", "研发部"}
  → 覆盖行：0,1,2,4
  → 共有行：0,1,2
  → D集合：{4}（误伤1个独有行）
```

#### 规律3: 前缀规律（Field = Prefix*）
```
检查：某个前缀（1-3个字符）能否覆盖所有共有行

示例：
  编码 = "A*"
  → 覆盖行：0,1,2（A001, A002, A003）
  → D集合：空（完美）✅
```

#### 规律4: 前缀枚举规律（Field ∈ {Prefix1*, Prefix2*, ...}）
```
检查：一个前缀集合（≤10个）能否覆盖所有共有行

示例：
  编码 ∈ {"A*", "B*"}
  → 覆盖行：0,1,2,3
  → 共有行：0,1,2
  → D集合：{3}（误伤1个独有行）
```

#### 补充：数值范围规律（仅数值型字段）
```
检查：某个阈值（>=, <=）能否覆盖所有共有行

示例：
  年龄 >= 32
  → 覆盖行：0,1,2（35, 32, 38）
  → D集合：空（完美）✅
```

### 第三步：构建字段链条（FieldChain）

对每个字段，将提炼出的规律按独有行集合大小排序，形成链条：

```
链条结构：
Root节点（D最小） → 节点2 → 节点3 → 节点4

特点：
1. 最多保留4个节点
2. D集合相同的节点去重（保留Values最简单的）
3. Root节点是最优规律（D最小）
4. 链条表示规律的优先级顺序
```

## 💡 实际应用示例

### 示例1：完美等于规律

**数据**：
| 行号 | 部门 | 城市 | 类型 |
|------|------|------|------|
| 0 | 技术部 | 北京 | 共有 |
| 1 | 技术部 | 上海 | 共有 |
| 2 | 技术部 | 广州 | 共有 |
| 3 | 市场部 | 北京 | 独有 |
| 4 | 研发部 | 上海 | 独有 |

**字段"部门"的链条**：
```
节点1 (Root): 部门 = "技术部"     → D={} (完美) ✅
节点2:        部门 ∈ {"技术部", "研发部"} → D={4}
节点3:        部门 ∈ {"技术部", "市场部"} → D={3}
节点4:        部门 ∈ {"技术部", "研发部", "市场部"} → D={3,4}
```

**解读**：
- **最优规律**：`部门 = "技术部"` 完美区分共有行和独有行
- 如果第一个规律不适用，可以尝试链条中的后续规律

### 示例2：前缀规律

**数据**：
| 行号 | 编码 | 部门 | 类型 |
|------|------|------|------|
| 0 | A001 | 技术部 | 共有 |
| 1 | A002 | 技术部 | 共有 |
| 2 | A003 | 技术部 | 共有 |
| 3 | B001 | 市场部 | 独有 |
| 4 | C001 | 研发部 | 独有 |

**字段"编码"的链条**：
```
节点1 (Root): 编码 = "A*"          → D={} (完美) ✅
节点2:        编码 = "A00*"        → D={} (完美) ✅
节点3:        编码 ∈ {"A*", "B*"} → D={3}
节点4:        编码 = "A0*"         → D={} (完美) ✅
```

**解读**：
- 有多个完美规律，选择最简单的：`编码 = "A*"`

### 示例3：枚举规律（无单值完美规律）

**数据**：
| 行号 | 状态 | 级别 | 类型 |
|------|------|------|------|
| 0 | 进行中 | P6 | 共有 |
| 1 | 已完成 | P7 | 共有 |
| 2 | 进行中 | P8 | 共有 |
| 3 | 已取消 | P6 | 独有 |
| 4 | 待审批 | P7 | 独有 |

**字段"状态"的链条**：
```
节点1 (Root): 状态 ∈ {"进行中", "已完成"}  → D={} (完美) ✅
节点2:        状态 = "进行中"             → D={2缺失} (不完美)
节点3:        状态 = "已完成"             → D={0,2缺失} (不完美)
```

**解读**：
- 单值无法覆盖，需要枚举规律
- 最优规律：`状态 ∈ {"进行中", "已完成"}`

## 🔧 技术实现

### 核心数据结构

```go
// FieldNode - 字段节点（一个规律）
type FieldNode struct {
    Field  string    // 字段名
    Values []string  // 取值或模式
    D      *RowSet   // 独有行集合（误伤的独有行）
    IsRoot bool      // 是否为链条根节点（最优）
}

// FieldChain - 字段链条（一个字段的多个规律）
type FieldChain struct {
    Field string       // 字段名
    Nodes []*FieldNode // 规律节点列表（按D大小排序）
    Root  *FieldNode   // 根节点（最优规律）
}
```

### 核心函数

```go
// 分析一个字段的所有规律
func analyzeFieldPatterns(field string, rows []Row, commonSet *RowSet) *FieldChain

// 4种规律分析函数
func analyzeEqualPatterns(stats *FieldStats, commonSet *RowSet) []*FieldNode
func analyzeEnumPatterns(stats *FieldStats, commonSet *RowSet, maxValues int) []*FieldNode
func analyzePrefixPatterns(stats *FieldStats, commonSet *RowSet) []*FieldNode
func analyzePrefixEnumPatterns(stats *FieldStats, commonSet *RowSet, maxPrefixes int) []*FieldNode
```

## 📈 性能优化

1. **预处理字段统计**：避免重复遍历行数据
2. **RowSet位图实现**：高效的集合运算（O(1)空间）
3. **去重策略**：D集合相同的规律只保留最简单的
4. **限制组合数量**：枚举/前缀枚举最多10个元素

## 🎯 输出格式

### 对每个Sheet，输出字段链条列表

```
主表过滤条件分析结果：

字段1: 部门
  ✅ 规律1 (Root): 部门 = "技术部"        [D=0, 完美]
  📋 规律2:        部门 ∈ {"技术部","研发部"} [D=1]
  📋 规律3:        部门 ∈ {"技术部","市场部"} [D=1]

字段2: 编码
  ✅ 规律1 (Root): 编码 = "A*"           [D=0, 完美]
  ✅ 规律2:        编码 = "A00*"         [D=0, 完美]

字段3: 年龄
  ✅ 规律1 (Root): 年龄 >= 32           [D=0, 完美]
```

## 🚀 下一步扩展

1. **组合规则**：多字段组合才能完美覆盖
2. **智能推荐**：基于D集合大小和规律复杂度评分
3. **可视化输出**：生成Excel分析报告
4. **交互式调整**：允许用户微调规律参数
